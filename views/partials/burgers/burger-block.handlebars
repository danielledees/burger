
			{{burger_name}}
		
		
			<button class="devourBtn btn btn-outline-secondary rounded-0" data-id="{{id}}" data-newdevour="{{devoured}}">
				{{#if devoured}}Devour It!{{else}}Reorder it!{{/if}}
			</button>
